# TradePilot Web — environment example
# Copy this file to `.env.local` and adjust as needed:  cp .env.example .env.local

# Base URL of the FastAPI backend. Server-side only (no NEXT_PUBLIC_ prefix),
# so it never ships to the browser.
API_BASE_URL=http://localhost:8000
